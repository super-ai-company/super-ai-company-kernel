__pycache__/
*.py[cod]
.DS_Store

company.sqlite
company.sqlite-*
logs/
state/

# Local secrets (Telegram/LINE tokens) — sourced by entry bins, never commit.
config/secrets.env
config/company_communications.json.bak-*

employees/*/heartbeat.json
employees/*/inbox/
employees/*/outbox/
employees/*/reports/
employees/ecommerce-copy-skill/

.ops/
design-system/
reports/heartbeats/
reports/communication-acceptance/
reports/tmp-chrome-profile/
reports/*.log
reports/antigravity-runs/
reports/backend-runs/
reports/codex-plans/
reports/codex-runs/
reports/frontend-runs/
reports/progress_*.json
reports/*-202606*.md

# One-off local patch generators whose effects are already committed.
scripts/patch_chat_hub_direct_messages.py
scripts/patch_external_mirror.py

.pytest_cache/
.coverage
htmlcov/

# Per-deployment runtime state — created/managed locally, never uploaded.
# Employees are added per-install via the onboarding skill, not shipped in the repo.
config/company_communications.json
employees/*/profile.json
employees/*/capabilities.json
employees/*/permissions.json
employees/*/rules.md
# junk / mount artifacts
*.fuse_hidden*
*.sqlite-journal
__mount_write_test.*
run-gemini-qa.sh

# Human RBAC users + tokens — per-deployment, never uploaded
config/users.json
